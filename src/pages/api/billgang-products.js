export default async function handler(req,res){const{categoryId=1}=req.query;const url=new URL(`https://pg-api.billgang.com/v1/dash/shops/3a31d58c-9273-4d74-bb78-2c55aad39453/products?categoryId=${categoryId}`);try{const response=await fetch(url,{headers:{Authorization:`Bearer ${process.env.NEXT_PUBLIC_BILLGANG_API_KEY}`},});if(!response.ok){console.error("Failed to fetch products from Billgang:",response.status,response.statusText);res.status(response.status).json({error:response.statusText});return}
const data=await response.json();res.setHeader('Cache-Control','no-store, no-cache, must-revalidate, proxy-revalidate');res.setHeader('Pragma','no-cache');res.setHeader('Expires','0');res.setHeader('Surrogate-Control','no-store');res.status(200).json(data.data)}catch(error){console.error("Error fetching product data:",error);res.status(500).json({error:'Error fetching product data'})}}